#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main (int argc, char* argv[]){
    /*char* string;
    char* decomposition;
    char token=atoi(" ");
    fgets(string,1000,argv[1]);
    decomposition=strtok(string, token);
    while (decomposition != NULL){
        printf("%s\n", decomposition);
        decomposition=strtok(NULL, token);
    }*/
    return 0;
}
